{
  "description": "Alfred configuration",
  "type": "object",
  "required": [
    "call_signs",
    "input_id",
    "output_id",
    "global_interaction_ids",
    "root_interaction_ids"
  ],
  "additionalProperties": false,
  "properties": {
    "call_signs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "input_id": {
      "type": "string"
    },
    "output_id": {
      "type": "string"
    },
    "root_interaction_ids": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "global_interaction_ids": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "locale": {
      "description": "Locale configuration. A locale identifier consists of an ISO 639 language code, optionally suffixed by a dash and an ISO 3316 country code",
      "type": "object",
      "properties": {
        "default": {
          "description": "The default locale. It will be available globally. Defaults to en-US.",
          "type": "string"
        },
        "outputs": {
          "description": "The available output locales. Defaults to the global default.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}